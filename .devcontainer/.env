RUST_BASE_IMAGE=app-rust-base:latest
WEBUI_HOST=client.localhost
WEBUI_PORT=443
API_HOST=api.localhost
API_PORT=50051
API_PORT_FOR_WASM=443 # [!TIP] FIXME API_PORT_FOR_WASM: actually WASM application is not going over http3 so it is binded to 443, caddy then reverse to 50051 of the GRPC api'server
CARGO_TARGET_DIR=/cargo_target